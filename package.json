{
  "name": "smartcar-javascript-sdk",
  "version": "0.0.24",
  "private": true,
  "scripts": {
    "test": "mocha-phantomjs test/test.html",
    "posttest": "npm run lint -s",
    "lint": "eslint . --cache",
    "cover": "nyc instrument src instrumented && mocha-phantomjs test/cover.html --hooks test/phantom_hooks.js && nyc report && rm -r instrumented && nyc check-coverage"
  },
  "devDependencies": {
    "browser-sync": "^2.18.5",
    "chai": "^4.0.2",
    "dirty-chai": "^2.0.0",
    "eslint": "^4.1.1",
    "eslint-config-smartcar": "^2.0.0",
    "gulp": "^3.8.5",
    "gulp-awspublish": "^3.0.1",
    "gulp-load-plugins": "^1.2.0",
    "gulp-rename": "^1.2.2",
    "gulp-uglify": "^3.0.0",
    "mocha": "^3.2.0",
    "mocha-phantomjs": "^4.0.2",
    "nyc": "^11.0.3",
    "sinon": "^2.3.6",
    "sinon-chai": "^2.8.0"
  }
}
